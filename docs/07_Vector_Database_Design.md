# Document 7 — Vector Database Design

## Out of Scope for the HADES Model-Development Prototype

Status: Out of scope

---

## 1. Why this document is empty

This document previously specified the vector store (`evidence_chunks`, hybrid search, re-indexing) backing RAG-grounded LLM explanations. This project is currently scoped to **model development only** — training and validating the HADES graph architecture (`project_HADES.md`). There is no RAG layer, no LLM explanation generation, and no evidence retrieval in this project's plan, so there is nothing for a vector store to back.

## 2. If this work resumes

`architecture.md` (Layer 3 — RAG) is the reference point if evidence-grounded explanation generation is picked back up later.

## 3. What to read instead

- **`10_AI_ML_Documentation.md`** — what is actually being built.
- **`14_Model_Development_Roadmap.md`** — the order it's being built in.

---

## Document Control

- Superseded by the model-development rescope. Not maintained until/unless RAG is back in scope.
